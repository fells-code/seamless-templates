name: validate

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    name: Plan template checks
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.matrix.outputs.matrix }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: npm

      - name: Install dependencies
        run: npm ci

      # npm run validate and npm run format:check are the pre-commit hook's job.
      # They need only the root's dependencies, run in about a second, and fail
      # on the machine that made the change rather than a few minutes later.
      # This job is left with the one thing a hook cannot do, which is tell the
      # matrix below what to build.
      - name: Compute build matrix
        id: matrix
        run: echo "matrix=$(node scripts/validate-templates.mjs --matrix)" >> "$GITHUB_OUTPUT"

  # Every step is --if-present so a template that has not adopted one of these yet
  # is skipped rather than failed. A template that does declare the script has to
  # pass it: this is the same gate `npm run check` gives a user locally.
  template-checks:
    name: Check ${{ matrix.id }}
    needs: validate
    if: needs.validate.outputs.matrix != '[]'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.validate.outputs.matrix) }}
    defaults:
      run:
        working-directory: ${{ matrix.path }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"

      - name: Install
        run: npm install

      - name: Typecheck
        run: npm run typecheck --if-present

      - name: Lint
        run: npm run lint --if-present

      - name: Format check
        run: npm run format:check --if-present

      - name: Test
        run: npm run test --if-present

      - name: Build
        run: npm run build --if-present
