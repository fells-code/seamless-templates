{
  "id": "react-oauth",
  "targetDir": "web",
  "env": {
    "fromExample": ".env.example",
    "set": {
      "VITE_API_URL": "{{apiUrl}}"
    }
  },
  "verify": {
    "project": "react",
    "flows": ["oauth"]
  },
  "setup": {
    "oauth": true
  },
  "requires": { "cliMin": "0.3.0" }
}
