{
  "id": "react-oauth",
  "targetDir": "web",
  "env": {
    "fromExample": ".env.example",
    "set": {
      "VITE_AUTH_SERVER_URL": "{{authServerUrl}}",
      "VITE_API_URL": "{{apiUrl}}"
    }
  },
  "verify": {
    "project": "react",
    "flows": ["oauth"]
  },
  "requires": { "cliMin": "0.3.0" }
}
